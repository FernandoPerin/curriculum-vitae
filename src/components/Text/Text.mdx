{/* Text.mdx */}

import { Canvas, Meta, Story } from "@storybook/blocks";

import * as TextStories from "./Text.stories";

<Meta of={TextStories} />

# Text

A Text is a square box that can be activated or deactivated when ticked.

Use Textes to select one or more options from a list of choices.

<Canvas of={TextStories.Example} />
